# Quiet Configuration — Example
# Copy to quiet_config.txt and edit for your instance.

[IDENTITY]
CLAUDE_NAME=your-name
MODEL=claude-opus-4-6
BUDGET=

[NETWORK]
COOP_URL=
HOSTNAME=

[AUTH]
AUTH_MODE=auto
HUMAN_NAME=

[DISPLAY]
SEPARATOR=· · ·

[AUTONOMOUS]
# Minutes between autonomous wakes. 0 = disabled.
# Requires quiet-timer.timer to be enabled.
#
# Note: autonomous wakes only fire on subscription auth (free at
# the margin) or when BUDGET is set (api auth with headroom).
# On api billing without a budget, the timer exits silently to
# avoid unexpected costs.
AUTONOMOUS_INTERVAL=60
